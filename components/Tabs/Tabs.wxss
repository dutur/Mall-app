.tabs .tabs_title{
  display: flex;
  padding: 15rpx 0;
}
.tabs .title_item{
  display: flex;
  justify-content: center;
  align-items: center;
  flex: 1;
  padding: 15rpx 0;
}
.tabs .active{
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}